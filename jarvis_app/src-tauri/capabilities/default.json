{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capabilities for the JARVIS control window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
