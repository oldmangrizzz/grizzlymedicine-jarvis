# Gaps

- Live production organ IPC socket paths in the default plan are placeholders until the runtime supervisor exposes stable UDS addresses.
- The orchestrator has a UDS endpoint implementation and synthetic test endpoints; end-to-end production organ traffic needs supervisor wiring.
- Documentation uses text diagrams/descriptions rather than image screenshots.
